// Unit tests for CAPS components

#include "gtest/gtest.h"
#include "sema.h"
#include "parser.h"

TEST(SemaTest, TypeCheck) {
  // Test type checking
  ASSERT_TRUE(true);
}

TEST(ParserTest, ParseExpr) {
  // Test parsing
  ASSERT_TRUE(true);
}